{
  "name": "@parallax/ai-wallet",
  "version": "0.1.0-alpha.0",
  "private": true,
  "type": "module",
  "description": "Policy-gated AI wallet domain package for PARALLAX alpha.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^2.1.9"
  }
}
